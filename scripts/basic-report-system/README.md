This is a basic reporting system. It sends a report to a discord webhook preferably, but you can do whichever type you'd like.
It's already got some pre-built json data so you don't  have to set anything up.

It includes:
  - Thumbnails for both the reporting, and reported player.
  - Profile links
  - 
  - Reason for report (I'll add reason validation so the client can't send whatever they want as the reason.)
  - Additional notes (I probably need to make this filtered, but right now it's not filtered.)

There's no rate-limting cause I've never done that before, but I will add it in the future once I figure it out.

